/* Quarry Sports Bar — runtime configuration.
   Phase 1 (demo): leave QUARRY_API as null — the Play page runs on sample data.
   Phase 2 (live): set QUARRY_API = '/api' after deploying the backend,
   then remove the demo banner from play.html. */
window.QUARRY_API = null;
